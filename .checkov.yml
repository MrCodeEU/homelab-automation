compact: true
quiet: true
skip-path:
  - .git
  - .githooks/.venv
  # Retained only as the OpenVox migration reference; it is no longer a
  # deployment path. Active infrastructure remains fully scanned.
  - ansible
  - docs/ansible-map.md
  - tests
