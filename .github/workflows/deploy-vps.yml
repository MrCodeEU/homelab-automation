name: Deploy VPS

on:
  workflow_dispatch:
    inputs:
      roles:
        description: 'Roles to deploy (all, base, docker, caddy)'
        required: true
        default: 'all'
        type: choice
        options:
          - all
          - base
          - docker
          - caddy
          - base,docker
          - base,docker,caddy

permissions:
  contents: read

jobs:
  deploy-vps:
    name: Deploy to VPS
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Tailscale
        uses: tailscale/github-action@v3
        with:
          oauth-client-id: ${{ secrets.TS_OAUTH_CLIENT_ID }}
          oauth-secret: ${{ secrets.TS_OAUTH_SECRET }}
          tags: tag:ci

      - name: Make scripts executable
        run: chmod +x scripts/*.sh

      - name: Test SSH connectivity
        run: |
          tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "echo '✓ VPS connection successful'"

      - name: Copy deployment scripts
        run: |
          tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "mkdir -p /tmp/homelab-deploy"
          tar czf - scripts/ configs/ | tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "cd /tmp/homelab-deploy && tar xzf -"
          echo "✓ Scripts and configs deployed"

      - name: Execute base setup
        if: contains(github.event.inputs.roles, 'base') || github.event.inputs.roles == 'all'
        run: |
          tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "bash /tmp/homelab-deploy/scripts/01-base-setup.sh rocky"
          echo "✓ Base setup completed"

      - name: Execute Docker setup
        if: contains(github.event.inputs.roles, 'docker') || github.event.inputs.roles == 'all'
        run: |
          tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "bash /tmp/homelab-deploy/scripts/02-docker-setup.sh rocky"
          echo "✓ Docker setup completed"

      - name: Deploy Docker Compose stacks
        if: contains(github.event.inputs.roles, 'docker') || github.event.inputs.roles == 'all'
        run: |
          tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "bash /tmp/homelab-deploy/scripts/03-docker-compose-deploy.sh rocky"
          echo "✓ Docker Compose stacks deployed"

      - name: Execute Caddy setup
        if: contains(github.event.inputs.roles, 'caddy') || github.event.inputs.roles == 'all'
        run: |
          tailscale ssh "${{ secrets.VPS_USER }}@${{ secrets.VPS_HOST }}" "bash /tmp/homelab-deploy/scripts/04-caddy-setup.sh rocky /tmp/homelab-deploy/configs/caddy/Caddyfile"
          echo "✓ Caddy setup completed"

      - name: Deployment summary
        if: always()
        run: |
          echo "### VPS Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "- **Device**: VPS" >> $GITHUB_STEP_SUMMARY
          echo "- **Roles**: ${{ github.event.inputs.roles }}" >> $GITHUB_STEP_SUMMARY
          echo "- **Status**: ${{ job.status }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          
          if [ "${{ job.status }}" == "success" ]; then
            echo "🎉 **VPS deployment successful!**" >> $GITHUB_STEP_SUMMARY
          else
            echo "❌ **VPS deployment failed. Check logs above.**" >> $GITHUB_STEP_SUMMARY
          fi
