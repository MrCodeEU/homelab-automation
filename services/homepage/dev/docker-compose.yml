services:
  homepage:
    image: ghcr.io/mrcodeeu/next-gen-web-dev/homepage:${IMAGE_TAG:-latest}
    container_name: ${COMPOSE_PROJECT_NAME:-homepage-staging}
    restart: unless-stopped
    ports:
      - "18082:8082"  # Staging port (+10000 offset)
    environment:
      - GITHUB_TOKEN=${GH_TOKEN}
      - STRAVA_CLIENT_ID=${STRAVA_CLIENT_ID}
      - STRAVA_CLIENT_SECRET=${STRAVA_CLIENT_SECRET}
      - STRAVA_REFRESH_TOKEN=${STRAVA_REFRESH_TOKEN}
      - LINKEDIN_EMAIL=${LINKEDIN_EMAIL}
      - LINKEDIN_PASSWORD=${LINKEDIN_PASSWORD}
      - LINKEDIN_TOTP_SECRET=${LINKEDIN_TOTP_SECRET}
      - SCRAPER_CACHE_FILE=/data/scraper-cache.json
    volumes:
      - homepage-data:/data
    networks:
      - homepage

networks:
  homepage:
    driver: bridge

volumes:
  homepage-data:
    driver: local
