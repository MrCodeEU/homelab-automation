---
# Backup role handlers

- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: Enable backup timer
  ansible.builtin.systemd:
    name: homelab-backup.timer
    enabled: true
    state: started
