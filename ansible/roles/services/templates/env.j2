# Auto-generated .env file
# Secrets injected by Ansible

# Nightscout
API_SECRET="{{ nightscout_api_secret_var }}"
LINK_UP_USERNAME="{{ link_up_username_var }}"
LINK_UP_PASSWORD="{{ link_up_password_var }}"
NIGHTSCOUT_API_TOKEN="{{ nightscout_api_token_var }}"
NIGHTSCOUT_DOMAIN="{{ nightscout_domain_var }}"

# Bichon
BICHON_ENCRYPT_PASSWORD="{{ bichon_encrypt_password_var }}"

# GitHub (for homepage service)
GITHUB_USERNAME="{{ github_username_var }}"
GITHUB_TOKEN="{{ github_token_var }}"

# Common
TZ="{{ timezone }}"
DOMAIN="{{ domain }}"
EMAIL="{{ email }}"

# Docker Image Tags (for apps)
IMAGE_TAG={{ 'dev' if is_staging_deployment | default(false) else 'latest' }}
