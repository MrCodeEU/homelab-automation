# Mailcow Docker Compose Override
# Managed by Ansible - do not edit manually
# Generated: {{ ansible_date_time.iso8601 | default('N/A') }}
#
# Note: Port configuration is handled via mailcow.conf:
#   HTTP_PORT={{ mailcow_http_port }}, HTTP_BIND={{ mailcow_http_bind }}
#   HTTPS_PORT={{ mailcow_https_port }}, HTTPS_BIND={{ mailcow_http_bind }}

services:
  nginx-mailcow:
    environment:
      - SKIP_LETS_ENCRYPT=y
      - SKIP_HTTP_VERIFICATION=y
      - ADDITIONAL_SERVER_NAMES={{ mailcow_hostname }}
