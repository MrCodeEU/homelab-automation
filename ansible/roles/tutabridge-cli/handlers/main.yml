---
- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true

- name: restart gnome-keyring-daemon
  ansible.builtin.systemd:
    name: gnome-keyring-daemon
    state: restarted
    daemon_reload: true
  when: not ansible_check_mode
