---
collections:
  - name: community.docker
    version: ">=3.0.0"
  - name: community.general
    version: ">=8.0.0"
  - name: ansible.posix
    version: ">=1.5.0"
  # openssh_keypair, used by roles/healthreport for the facts-endpoint key
  - name: community.crypto
    version: ">=2.0.0"
