services:
  example-service:
    image: nginx:alpine
    container_name: example-service
    restart: unless-stopped
    ports:
      - "8888:80"
    volumes:
      - example-data:/usr/share/nginx/html
    environment:
      - TZ=Europe/London

volumes:
  example-data:


